This folder contains the Snake_Gym environment.
This also contains the PPO agent used to play the Snake game.
Also contains the trained agent and the file where one can load the trained agent and see it in action.
