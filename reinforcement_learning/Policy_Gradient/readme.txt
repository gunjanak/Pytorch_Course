Implementation of Policy gradient using PyTorch

Policy Gradient refers to a class of reinforcement learning algorithms
where the objective is to directly parameterize the policy 
(a mapping from states to actions) and optimize these parameters to maximize cumulative reward.
